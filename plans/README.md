# Plans

Drop Markdown (`.md`) project plans here. The orchestrator picks up new files, registers them in SQLite, and runs the **planner → coder → reviewer** pipeline.

See the main [README](../README.md) for usage.
