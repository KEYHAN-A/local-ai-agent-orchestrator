# Optional environment overrides (copy to .env and use a dotenv loader, or export manually)
LM_STUDIO_BASE_URL=http://127.0.0.1:1234
OPENAI_API_KEY=lm-studio
# LAO_CONFIG=/path/to/factory.yaml
# TOTAL_RAM_GB=36
# Per-plan code lives under <config_dir>/<plan-stem>/ (stem of plans/Foo.md -> ./Foo/)
# WORKSPACE_ROOT=./.lao/_misc
# PLANS_DIR=./plans
# DB_PATH=./.lao/state.db
