.venv/
venv/
# Local LAO scratch directory (not tests/ package tests)
/test/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.env
# Local LM Studio config (use factory.example.yaml in-repo)
factory.yaml
factory.yml
# Local PyPI / secrets notes (see docs/PYPI_PUBLISH.md for the public guide)
PYPI_PUBLISH.local.md
# LAO runtime: DB, WAL, per-plan workspaces, caches — keep repo root clean
.lao/
state.db
state.db-shm
state.db-wal
# Misconfigured DB path (SQLite sometimes creates these as literal "None")
None
None-shm
None-wal
*.sqlite
.cursor/
.DS_Store
*.log

# Legacy single workspace (optional)
workspace/*
!workspace/.gitkeep

# Optional: local test plans
# plans/*.md
